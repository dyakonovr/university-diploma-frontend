/** Max-width for binary artifact preview in a single block view */
export const BLOCK_BINARY_ARTIFACT_MAX_WIDTH = '600px';

/** Max-width for binary artifact preview in all-artifacts grid (2-column layout) */
export const ALL_ARTIFACTS_BINARY_ARTIFACT_MAX_WIDTH = '300px';
