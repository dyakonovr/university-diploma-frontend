1. Create GET /integrations/{id}
2. Now for commands let's create interface. For first time we don't have AI and we will simulate it's work. We should check messages by constant templates and emulate that AI is working

